{
  "name": "myra-connector-gitlab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitLab connector for Myra Agents — trigger patrols from pushes/merge requests/issues, open merge requests with the result, and a GitLab MCP tool. Built on the shared connector SDK.",
  "engines": { "node": ">=18" },
  "scripts": {
    "connect": "node connect.mjs",
    "mcp": "node mcp.mjs"
  }
}
